/** Where global setup leaves the signed-in staff context. Gitignored: it is a
 *  derived token, not a fixture. */
export const STAFF_AUTH_FILE = 'e2e/.staff-auth.json';

/** The passcode the e2e server treats as being rotated away from (C-158). */
export const ROTATED_OUT_PASSCODE = 'e2e-rotated-out';
