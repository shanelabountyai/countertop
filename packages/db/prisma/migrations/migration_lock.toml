# Please do not edit this file manually
# It should be added in your repository (.git) and tracked by version control.
# It tells Prisma which connector the committed migration history targets —
# without it `prisma migrate diff --from-migrations` cannot determine the
# connector, and CI's drift check fails before the first migration exists.
provider = "postgresql"
